#include "vmd_converter.h"
namespace enishi::animation {
    types::Keyframes<int> FrameConverter::to_keyframes(const assets_system::VMDData& data) {
        data.bone_key_frames;

        return types::Keyframes<int>();
    }
} // namespace enishi::animation